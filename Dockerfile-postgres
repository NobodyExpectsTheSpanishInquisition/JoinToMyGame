FROM postgres:10

ARG DB_PASSWORD
ARG DB_USER
ARG DB_DATABASE
ARG DB_EXTERNAL_PORT

EXPOSE $DB_EXTERNAL_PORT:5432

ENV POSTGRES_PASSWORD = $DB_PASSWORD
ENV POSTGRES_USER = $DB_USER
ENV POSTGRES_DB = $DB_DATABASE
